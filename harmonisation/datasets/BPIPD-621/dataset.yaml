dataset_id: "TEMPLATE_ID"
dataset_name: "Template Study"
status: "in_progress"

resources:
  - name: "data"
    role: "data"
    glob: "data.csv"
    reader: "csv"

notes: |
  Copy the matching dataset templates from:
  - harmonisation/templates/dataset/variables_TEMPLATE.csv
  Create `R/tidy/tidy_BPIPD_<id>.R` for this dataset. The pipeline expects one
  tidier file per study and tracks that file directly.
  Prefer inline `dplyr::recode()` in `variables.csv` for short one-off
  recodes. Put common cross-study recodes (for example ethnicity category
  mappings) in `harmonisation/lookups/*.csv`. Create a dataset-specific
  `lookups/` folder only when a large one-off mapping is not shared across
  studies and is too large to keep inline.
